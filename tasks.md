# Project Tasks

## Pending

- **Progressive Render Levels** — In-app debug tool that builds form rendering in 5 layers: (1) Chrome, (2) Layout, (3) Data Source, (4) Data Binding, (5) Interactivity. Dropdown in form view toolbar, info bar when level < 5. Also exposes level to LLM chat context. Plan in `skills/render-levels.md`. Implement forms first; same architecture reusable for reports/tables/queries later.
- **React UI debugging** — React is now the default frontend. Known issues from March 25 smoke test:
  1. Tab bar shows raw filenames — need `filenameToDisplayName` in tab bar/Header component
  2. Record position "0 of 5" — off-by-one in form view record navigation
  3. Subform column order mismatch — header vs detail band columns in frmAdmin System Settings
  4. General smoke testing: report preview, query execution, chat panel, module/macro viewers, full import flow
- **Self-healing pipeline Phase 1 integration** — Server-side evaluation ledger complete (4 pipeline tables + standalone `shared.evaluations` table, evaluator lib, 6 pipeline endpoints + 4 evaluation endpoints, 30 tests). Remaining: (1) Add `_saveAsText` capture to `export_form.ps1`/`export_forms_batch.ps1` and `export_report.ps1`/`export_reports_batch.ps1` (2 lines each), (2) Wire pipeline evaluation into React import flow (calls reconnaissance → capture-artifacts → generate-intents → evaluate after wire-events, before validation), (3) Test with live northwind import to verify evaluation catches the frmAbout regression.
- **Self-healing pipeline Phase 2** — Automated repair. Build on evaluation ledger: when evaluation fails, attempt LLM-powered repair (like autofix-pass but guided by intents). Expand to reports/modules/macros. Wire `parent_task_id` for sub-object granularity.
- **Expand VBA-to-JS parser coverage** — If/Else control flow done (March 23). Remaining: Select Case → switch, For/Do/While loops, DLookup, variables. Also need `AC.getVisible()`/`AC.getEnabled()` runtime getters to translate visibility/enabled conditions. Test against cranbrook and northwind modules to identify gaps.
- **SQL Server data layer support** — Many Access deployments use SQL Server as the back-end (linked tables). Support connecting to an existing SQL Server for user data while keeping platform metadata in PostgreSQL. Requires: dual connection manager, T-SQL query adapter on CRUD routes, T-SQL output mode for query converter, SQL Server metadata queries. Form state sync cross-joins need special attention. See MEMORY.md for full assessment.
- **Image import test** — Run `POST /api/database-import/import-images` against northwinddev.accdb and verify images render in forms.
- **Runtime form state sync test** — Open a form, navigate records, verify `form_control_state` populates and dependent views filter.
- **Trace `[object Object]` database_id bug** — 16 orphan rows found in forms/reports with `database_id = '[object Object]'`. Some code path is passing a JS object instead of a string slug. Cleaned up data but root cause unknown.
- **Re-import existing stubs after generator fix** — The VBA stub generator fix (ParamArray/enum) only helps new stubs. Existing databases may have stubs with wrong signatures. Consider adding a "regenerate stubs" button or making `createStubFunctions` optionally overwrite existing stubs.

## In Progress

## Completed

- **Standalone evaluation system** (March 27) — New `shared.evaluations` table (decoupled from pipeline tables). Every form/report save runs deterministic checks and records results. `runAndRecordEvaluation` orchestrator in `pipeline-evaluator.js`. New `server/routes/evaluations.js` with 4 endpoints: `POST /run-all` (batch), `POST /:type/:name/run` (single), `GET /:type/:name` (history), `GET /:type/:name/latest`. Report checks added (`runReportDeterministicChecks`). 11 new tests. 647 tests pass.
- **React hook order fix** (March 27) — `useMemo` in `FormProperties.tsx` was after a conditional `return null`, violating Rules of Hooks. Moved before all early returns. Caused "Rendered more hooks than during the previous render" crash.

- **Self-Healing Pipeline: Evaluation Ledger (Phase 1 — Forms)** (March 24) — 4 new tables (`pipeline_steps`, `pipeline_tasks`, `pipeline_task_attempts`, `pipeline_task_evaluations`) + ALTER `import_runs` for `app_profile`. New `server/lib/pipeline-evaluator.js` reuses lint validators for deterministic checks + artifact invariant checks + LLM semantic evaluation. New `server/routes/database-import/pipeline.js` with 6 endpoints (reconnaissance, capture-source-artifacts, generate-intents, evaluate, pipeline-status, pipeline-task). 18 pipeline steps seeded. 19 new tests. All 636 tests pass. Phase 1 = forms only, evaluation ledger only, no automated repair.
- **Unified Objects Table + Intents Table** (March 24) — Consolidated `shared.forms`, `shared.reports`, `shared.modules`, `shared.macros` into `shared.objects` with `type` discriminator column. Separated intents into `shared.intents` table with FK. Module/macro columns packed into `definition` JSONB. New `server/lib/objects.js` helpers. ~30 files updated. API unchanged, frontend untouched. 620 tests pass. Needs live verification (import a database, check counts, verify viewers).
- **VBA-to-JS parser expanded coverage** (March 24) — Select Case → switch, numeric For loops, variable tracking (Dim + assignments), enhanced conditions (Me.NewRecord, Me.Dirty, IsNull, Me.ctrl.Visible), translateAssignmentRHS, AC.nz runtime. 118 tests (up from 52).
- **VBA-to-JS parser If/Else control flow** (March 23) — Added `translateCondition`, `translateBlock`, `parseIfBlock`, `findEndKeyword`. Parser recognizes block If/ElseIf/Else/End If, single-line If, skips Select Case/For/Do/While/With as comments. Untranslatable conditions (local vars, Me.NewRecord, IsNull) cause entire block to be commented out. Line continuations merged in `stripBoilerplate`. 52 new tests. Fixes frmCompanyDetail Form_Load bug.
- **Re-Import enhanced with query re-conversion, module translation, and validation** (March 22) — `reimport-object!` now runs 3 post-import steps: re-converts record-source query, re-translates class module (`Form_X`/`Report_X`) via `translate-modules` with new `module_names` filter, runs repair + validation passes. Server: `translate-modules.js` accepts optional `module_names` array.
- **VBA stub generator: ParamArray + enum type fix** (March 22) — `ParamArray` params excluded from stub signatures (optional/variadic in VBA). Enum types detected via `collectEnumNames()` and mapped to `integer`. Declaration regex handles nested parens. Fixed `getstring(integer)` error blocking `frmAbout`. 7 new tests (19 total).
- **Dead CLJS translation pipeline removed** (March 21) — Deleted `vba-wiring-generator.js` + tests, `intent_interpreter.cljs`. Removed `generate-wiring` endpoint, `update_translation` chat tool, CLJS panel in Module/Macro Viewer, "Generate All Code" step in App Viewer. Pipeline reduced from 5→4 steps. Module Viewer now shows VBA + JS handlers panel. All `cljs-source`/`cljs-dirty?` state paths cleaned. Forward declarations added for `load-chat-transcript!` and `fire-report-event!` — frontend compile warnings reduced from 9→3 (remaining are `:redef` from third-party lib + intentional `run!` shadow). ~27 files modified, 3 deleted. Updated CLAUDE.md, HANDOFF.md, 6 skills docs.
- **VBA-to-JS event handler architecture** (March 21) — Replaced intent-based runtime execution with JavaScript generated from VBA at import time. New files: `server/lib/vba-to-js.js` (parser), `ui/src/app/runtime.cljs` (window.AC API). JS handlers stored in `shared.modules.js_handlers` JSONB column, auto-generated on module save. All 511 existing modules backfilled. Intent interpreter removed from all event execution paths. Also fixed: command-button renderer, label pointer-events, SQL record-source routing.
- **Import UX flow fix** (March 20) — Moved "New Database" into database dropdown, disabled Import button when no source/target selected, added error messages to `trigger-import!`, suppressed table-load errors in import mode.
- **SaveAsText rewrite of all export scripts** (March 18) — Rewrote export_form.ps1, export_forms_batch.ps1, export_report.ps1, export_reports_batch.ps1 from COM design-view to Application.SaveAsText parsing. Form parser uses context stack for nested labels/tab controls. Report parser simpler (flat control depth).
- **Macro export reliability** (March 18) — Added `$accessApp.Visible = $false` to export_macro.ps1 and export_macros_batch.ps1. Changed batch timeout formula in export.js.
- **cranbrook14 full pipeline import** (March 18) — All 33 forms with controls, all 7 reports with controls, 38 modules translated, queries imported with dependency retry. First fully successful complex .mdb import.
- **Business intent extraction** (March 17) — LLM extracts purpose/category/data-flows from forms, reports, queries. Stored in JSONB, integrated into chat context.
