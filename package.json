{
  "name": "accessclone",
  "version": "0.2.0",
  "description": "Convert Microsoft Access databases to modern web applications — PostgreSQL, Node.js, React",
  "private": true,
  "scripts": {
    "test": "cd server && npm test && cd ../electron && npm test",
    "test:db": "cd server && npm run test:db",
    "start": "cd server && npm start"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kentyler/accessclone.git"
  }
}
