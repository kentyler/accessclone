{
  "name": "polyaccess",
  "version": "0.2.0",
  "description": "Low-code platform for converting MS Access databases to web applications",
  "private": true,
  "scripts": {
    "test": "cd server && npm test",
    "test:db": "cd server && npm run test:db",
    "start": "cd server && npm start",
    "build:ui": "cd ui && npx shadow-cljs compile app"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kentyler/PolyAccess.git"
  }
}
